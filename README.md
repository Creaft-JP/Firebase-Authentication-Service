# TitHub Authentication

## 概要

Firebaseによる認証機構のマイクロサービス

## 使用方法

### Credentialファイルの設定

./resource/firebase-credentials.jsonにFirebaseの認証情報のJSONファイルを配置する。

### Redis・Cassandraの起動（Docker）

```shell
docker compose up -d
```
